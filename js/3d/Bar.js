
class Bar {

    constructor(scene, clock) {
        this.scene = scene;
        this.clock = clock;
    }

    setPosition(section, bar) {
        this.section = section;
        this.bar = bar;
    }

    animate() {

    }
}

export default Bar;