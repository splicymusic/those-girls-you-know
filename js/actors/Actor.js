
class Actor {

    constructor(scene, clock, loader) {
        this.scene = scene;
        this.clock = clock;
        this.loader = loader;
    }

    update(cameraPosition, fpsAdjustment) {

    }
}

export default Actor;